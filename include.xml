<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- Manifest -->
  <project path="manifest" name="platform_manifest" remote="staging" />

  <!-- Build -->
  <project path="bionic" name="platform_bionic" remote="staging" />
  <project path="build/make" name="platform_build" remote="staging" groups="pdk" >
    <copyfile src="core/root.mk" dest="Makefile" />
    <linkfile src="CleanSpec.mk" dest="build/CleanSpec.mk" />
    <linkfile src="buildspec.mk.default" dest="build/buildspec.mk.default" />
    <linkfile src="core" dest="build/core" />
    <linkfile src="envsetup.sh" dest="build/envsetup.sh" />
    <linkfile src="target" dest="build/target" />
    <linkfile src="tools" dest="build/tools" />
  </project>
  <project path="build/soong" name="platform_build_soong" remote="staging" groups="pdk,tradefed" >
    <linkfile src="root.bp" dest="Android.bp" />
    <linkfile src="bootstrap.bash" dest="bootstrap.bash" />
  </project>

  <!-- System -->
  <project path="system/core" name="platform_system_core" remote="staging" />
  <project path="system/bt" name="platform_system_bt" remote="staging" />

  <!-- Frameworks -->
  <project path="frameworks/base" name="platform_frameworks_base" remote="staging" />
  <project path="frameworks/av" name="platform_frameworks_av" remote="staging" />
  <project path="frameworks/native" name="platform_frameworks_native" remote="staging" />

  <!-- Vendor -->
  <project path="vendor/aosap" name="platform_vendor_aosap" remote="staging" />

  <!-- Hardware -->
  <project path="hardware/libhardware" name="platform_hardware_libhardware" remote="staging" />

  <!-- Prebuilts -->
  <project path="prebuilts/abi-dumps/vndk" name="platform_prebuilts_abi-dumps_vndk" remote="staging" />

</manifest>
